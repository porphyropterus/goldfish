[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8.4"
tokio = { version = "1.45.1", features = ["rt-multi-thread"] }
cxx = "1.0"
serde = { version = "1.0.219", features = ["derive"] }

[build-dependencies]
cxx-build = "1.0"

